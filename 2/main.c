#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);

    printf("Multiplicación: %d \n", a * b);
    printf("División: %d", a / b);

    return 0;
}
